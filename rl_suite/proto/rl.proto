syntax = "proto3";

package rl;

message Observation {
    repeated float proprioception = 1;
    repeated float images = 2;
    repeated float metadata = 3;
}

message Episode {
    repeated Observation observations = 1;
    repeated float actions = 2;
    repeated float rewards = 3;
    repeated int32 dones = 4;
}
